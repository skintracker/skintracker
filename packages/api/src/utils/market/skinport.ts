import { STSkin } from "@skintracker/types/src";

export async function getMinPrice(skin: STSkin): Promise<string> {
  // biome-ignore lint/correctness/noConstantCondition: force to reject
  if (true) {
    Promise.reject("not implemented");
  }
  return "N/A";
}
