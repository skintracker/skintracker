{
	"name": "skintracker",
	"module": "index.ts",
	"type": "module",
	"version": "2023.3.1",
	"private": true,
	"workspaces": ["packages/api", "packages/types"],
	"scripts": {
		"build": "bun run --cwd packages/api build",
		"dev": "bun run --cwd packages/api dev",
		"prestart": "bun run --cwd packages/api build",
		"start": "bun run packages/api/dist/index.js"
	},
	"devDependencies": {
		"bun-types": "latest",
		"concurrently": "^8.2.1"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
